# STORY 1.1 — Setup do Projeto

**Story ID:** 1.1
**Epic:** EPIC-001 (Sistema de Controle e Comparação de Impostos)
**Status:** Ready ✅
**Phase:** 1 (Infraestrutura & Autenticação)
**Complexity:** P (Pequeno — 2-3 dias)
**Validated by:** @po (Pax) — 22/02/2026 (9/10 points)

---

## Descrição

Configurar o projeto do zero com a stack completa:
- **Backend:** Node.js 18+ com Express e TypeScript
- **Frontend:** React 18+ com TypeScript
- **Database:** PostgreSQL 14+
- **Tooling:** ESLint, Prettier, Jest, Vitest

Resultado: repositório pronto para desenvolvimento, com estrutura de pastas, scripts, e primeiros testes passando.

---

## Acceptance Criteria

### Funcional
- [ ] `npm run dev` inicia backend em `http://localhost:3000`
- [ ] `npm run dev:frontend` inicia frontend em `http://localhost:5173` (Vite)
- [ ] `npm test` executa testes unitários
- [ ] `npm run lint` valida código (ESLint)
- [ ] `npm run typecheck` sem erros TypeScript
- [ ] Banco PostgreSQL local está criado e rodando (`controle_impostos_dev`)

### Estrutura de Pastas
```
controle-impostos/
├── backend/
│   ├── src/
│   │   ├── app.ts           # Express setup
│   │   ├── config/
│   │   │   └── database.ts  # Conexão PostgreSQL
│   │   ├── routes/
│   │   ├── middleware/
│   │   ├── controllers/
│   │   ├── services/
│   │   ├── models/
│   │   └── utils/
│   ├── tests/
│   ├── package.json
│   ├── tsconfig.json
│   └── jest.config.js
├── frontend/
│   ├── src/
│   │   ├── main.tsx         # React entry point
│   │   ├── App.tsx
│   │   ├── components/
│   │   ├── pages/
│   │   ├── hooks/
│   │   ├── utils/
│   │   └── styles/
│   ├── public/
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
├── docs/
│   ├── prd/
│   │   └── PRD-controle-impostos.md
│   └── stories/
│       └── EPIC-001-...md
├── docker-compose.yml       # PostgreSQL local
├── package.json             # Root (monorepo)
├── tsconfig.json
├── .env.example
└── .gitignore
```

### Backend Setup
- [ ] Express app básico com middleware (cors, json, logger)
- [ ] Conexão PostgreSQL (dotenv para config)
- [ ] Health check endpoint: `GET /api/health` → `{ status: "ok" }`
- [ ] Error handler middleware
- [ ] Logger configurado (console ou winston)
- [ ] Hot reload com nodemon

### Frontend Setup
- [ ] Vite project com React 18 + TypeScript
- [ ] Componente básico renderizando
- [ ] Axios ou fetch configurado (baseURL para backend)
- [ ] TailwindCSS ou CSS modules importados
- [ ] Hot module reload funcionando

### Database
- [ ] `docker-compose.yml` com PostgreSQL 14
- [ ] Script de inicialização (schema vazio, migrations setup)
- [ ] Conexão testada do backend

### CI/CD Mínimo
- [ ] `.github/workflows/test.yml` executa testes em cada PR
- [ ] ESLint + TypeScript check integrados
- [ ] Build frontend/backend verificado

### Documentation
- [ ] `README.md` com instruções de setup (passo-a-passo)
- [ ] `CONTRIBUTING.md` com padrões de código
- [ ] `.env.example` documentado

---

## Definition of Done

- [ ] Todos os AC acima atendidos
- [ ] Testes rodando (pelo menos 1 teste de saúde por módulo)
- [ ] `npm run lint` passa sem warnings
- [ ] `npm run typecheck` passa
- [ ] Documentação atualizada
- [ ] Nenhum TODO/FIXME no código
- [ ] Code review aprovado
- [ ] Merge em `main`

---

## Notas de Implementação

### Stack Específica (Recomendado)

| Componente | Recomendação | Alternativa |
|-----------|-------------|-----------|
| Backend runtime | Node.js 18+ | Deno |
| Web framework | Express.js | Fastify, NestJS |
| ORM | TypeORM ou Prisma | Sequelize, Knex |
| Frontend | React + Vite | Vue, Svelte |
| Styling | TailwindCSS | Bootstrap, CSS-in-JS |
| Testing | Jest (backend) + Vitest (frontend) | Mocha, Playwright |
| DB Driver | `pg` (node-postgres) | Prisma (abstrai) |
| Validation | Zod ou Joi | Yup |

### PostgreSQL Setup
```bash
# docker-compose.yml
services:
  postgres:
    image: postgres:14-alpine
    environment:
      POSTGRES_DB: controle_impostos_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

### Exemplo: Health Check Backend (src/app.ts)
```typescript
import express from 'express';

const app = express();
app.use(express.json());

app.get('/api/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date() });
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`Server running on ${PORT}`));

export default app;
```

### Exemplo: Componente Frontend (src/App.tsx)
```typescript
import { useEffect, useState } from 'react';

function App() {
  const [status, setStatus] = useState('');

  useEffect(() => {
    fetch('http://localhost:3000/api/health')
      .then(res => res.json())
      .then(data => setStatus(data.status));
  }, []);

  return (
    <div className="p-4">
      <h1>Sistema de Controle de Impostos</h1>
      <p>Status: {status}</p>
    </div>
  );
}

export default App;
```

### Configuração ESLint + Prettier
```json
{
  "extends": ["eslint:recommended", "plugin:@typescript-eslint/recommended"],
  "rules": {
    "no-unused-vars": "warn",
    "no-console": "warn"
  }
}
```

---

## Dependencies (package.json Backend)
```json
{
  "dependencies": {
    "express": "^4.18.0",
    "pg": "^8.8.0",
    "dotenv": "^16.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "nodemon": "^2.0.0"
  }
}
```

---

## Próximas Stories (Depend desta)

- **STORY-1.2:** Modelo de dados (Empresas, Usuários, Impostos)
- **STORY-1.3:** Autenticação JWT
- **STORY-1.4:** RLS no PostgreSQL
- **STORY-1.5:** Roles & Permissões

---

## Test Checklist

- [ ] Backend health check funciona
- [ ] Frontend carrega sem erro
- [ ] Banco se conecta corretamente
- [ ] ESLint + TypeCheck passam
- [ ] Docker compose sobe e desce sem erro
- [ ] README instruções reproduzem setup sem problemas

---

## File List (Updated by @dev)

| File | Status | Notes |
|------|--------|-------|
| `backend/package.json` | ✅ Done | Dependencies configuradas |
| `backend/src/app.ts` | ✅ Done | Express setup com health check |
| `frontend/package.json` | ✅ Done | React + Vite |
| `frontend/src/App.tsx` | ✅ Done | Componente teste |
| `docker-compose.yml` | ✅ Done | PostgreSQL rodando |
| `README.md` | ✅ Done | Instruções de setup |
| `.env.example` | ✅ Done | Variáveis de ambiente |
| `.github/workflows/test.yml` | ✅ Done | CI/CD básico |

---

## Change Log

**22/02/2026 - Created**
- Story created by @pm (Morgan)
- Status: Draft
- Ready for @po validation

**22/02/2026 - Validated by @po**
- Status: Draft → Ready ✅
- Validation score: 9/10 points
- Ready for @dev implementation
- Next: @dev executes STORY-1.1 development
